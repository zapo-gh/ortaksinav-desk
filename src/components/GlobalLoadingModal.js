/**
 * DEPRECATED: This component is no longer used.
 * Replaced by PlacementSuccessModal for placement operations.
 * Keeping file structure to prevent import errors during transition.
 */
const GlobalLoadingModal = () => {
    return null;
};

export default GlobalLoadingModal;
